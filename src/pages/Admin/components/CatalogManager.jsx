import React, { useEffect, useMemo, useRef, useState } from "react";
import Papa from "papaparse";
import { addDoc, collection, deleteDoc, doc, getDocs, onSnapshot, query, serverTimestamp, updateDoc, where, writeBatch } from "firebase/firestore";
import { Edit3, Filter, FolderPlus, Plus, RefreshCw, Search, Trash2, Upload, X } from "lucide-react";
import { db } from "../../../components/Firebase";

const config = { Categories: ["categories", "Category"], SubCategories: ["subcategories", "Sub Category"], Collections: ["collections", "Collection"], Countries: ["countries", "Country"], Brands: ["brands", "Brand"], Attributes: ["attributes", "Attribute"] };
const tone = (status) => status === "Draft" ? "bg-amber-50 text-amber-700" : status === "Trash" ? "bg-rose-50 text-rose-700" : "bg-emerald-50 text-emerald-700";

const CatalogManager = ({ type }) => {
  const [collectionName, singular] = config[type]; const csvInput = useRef(null); const editor = useRef(null);
  const [sourceItems, setSourceItems] = useState([]); const [trashItems, setTrashItems] = useState([]); const [editing, setEditing] = useState(null); const [name, setName] = useState(""); const [description, setDescription] = useState(""); const [status, setStatus] = useState("Active"); const [search, setSearch] = useState(""); const [filter, setFilter] = useState("All"); const [error, setError] = useState(""); const [saving, setSaving] = useState(false);
  const toRows = (snapshot, isTrash = false) => snapshot.docs.map((item) => ({ id: item.id, ...(isTrash ? item.data().data : item.data()), status: isTrash ? "Trash" : (item.data().status || "Active"), trashId: isTrash ? item.id : null }));
  useEffect(() => {
    const stopSource = onSnapshot(collection(db, collectionName), (snapshot) => setSourceItems(toRows(snapshot)), (event) => setError(event.message));
    const stopTrash = onSnapshot(query(collection(db, "trash"), where("sourceCollection", "==", collectionName)), (snapshot) => setTrashItems(toRows(snapshot, true)), (event) => setError(event.message));
    return () => { stopSource(); stopTrash(); };
  }, [collectionName]);
  const items = useMemo(() => [...sourceItems, ...trashItems], [sourceItems, trashItems]);
  const reset = () => { setEditing(null); setName(""); setDescription(""); setStatus("Active"); setError(""); };
  const openAdd = () => { reset(); setTimeout(() => editor.current?.scrollIntoView({ behavior: "smooth", block: "center" }), 0); };
  const refresh = async () => { setSaving(true); setError(""); try { const [source, trash] = await Promise.all([getDocs(collection(db, collectionName)), getDocs(query(collection(db, "trash"), where("sourceCollection", "==", collectionName)))]); setSourceItems(toRows(source)); setTrashItems(toRows(trash, true)); } catch (event) { setError(event.message || "Refresh failed."); } finally { setSaving(false); } };
  const save = async (event) => { event.preventDefault(); if (!name.trim()) { setError(`${singular} name is required.`); return; } setSaving(true); try { const data = { name: name.trim(), description: description.trim(), status, updatedAt: serverTimestamp() }; if (editing) await updateDoc(doc(db, collectionName, editing.id), data); else await addDoc(collection(db, collectionName), { ...data, createdAt: serverTimestamp() }); reset(); } catch (event) { setError(event.message || "Unable to save."); } finally { setSaving(false); } };
  const edit = (item) => { if (item.trashId) return; setEditing(item); setName(item.name || ""); setDescription(item.description || ""); setStatus(item.status); setError(""); editor.current?.scrollIntoView({ behavior: "smooth", block: "center" }); };
  const moveToTrash = async (item) => { if (!window.confirm(`Move ${item.name} to Trash?`)) return; try { const { id, trashId, ...data } = item; const batch = writeBatch(db); const ref = doc(collection(db, "trash")); batch.set(ref, { sourceCollection: collectionName, sourceId: id, data, deletedAt: serverTimestamp() }); batch.delete(doc(db, collectionName, id)); await batch.commit(); if (editing?.id === id) reset(); } catch (event) { setError(event.message || "Unable to move to Trash."); } };
  const permanentDelete = async (item) => { if (!window.confirm(`Permanently delete ${item.name}? This cannot be undone.`)) return; try { await deleteDoc(doc(db, "trash", item.trashId)); } catch (event) { setError(event.message || "Unable to permanently delete."); } };
  const importCsv = (file) => { if (!file) return; Papa.parse(file, { header: true, skipEmptyLines: true, complete: async ({ data }) => { const rows = data.map((row) => ({ name: String(row.name || row.Name || "").trim(), description: String(row.description || row.Description || "").trim(), status: row.status === "Draft" ? "Draft" : "Active" })).filter((row) => row.name); if (!rows.length) { setError("CSV needs a name column with at least one value."); return; } setSaving(true); try { await Promise.all(rows.map((row) => addDoc(collection(db, collectionName), { ...row, createdAt: serverTimestamp(), updatedAt: serverTimestamp() }))); } catch (event) { setError(event.message || "CSV import failed."); } finally { setSaving(false); file.value = ""; } } }); };
  const counts = { total: items.length, active: sourceItems.filter((item) => item.status === "Active").length, draft: sourceItems.filter((item) => item.status === "Draft").length, trash: trashItems.length };
  const filtered = items.filter((item) => (filter === "All" || item.status === filter) && `${item.name} ${item.description || ""}`.toLowerCase().includes(search.toLowerCase()));
  return <div className="space-y-5"><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Total", counts.total, "bg-rose-50 text-[#b13896]"], ["Active", counts.active, "bg-emerald-50 text-emerald-600"], ["Draft", counts.draft, "bg-amber-50 text-amber-600"], ["Trash", counts.trash, "bg-slate-100 text-slate-600"]].map(([label, value, tint]) => <article key={label} className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><div className="flex justify-between"><div><p className="text-[14px] font-semibold text-slate-500">{label} {type.replace(/([A-Z])/g, " $1").trim()}</p><p className="mt-2 text-3xl font-bold">{value}</p></div><span className={`grid h-10 w-10 place-items-center rounded-full ${tint}`}><FolderPlus size={18}/></span></div><p className="mt-3 text-[14px] text-slate-400">Live Firestore count</p></article>)}</section><section className="overflow-hidden rounded-2xl border border-slate-100 bg-white shadow-sm"><div className="flex flex-col justify-between gap-4 border-b border-slate-100 px-6 py-5 sm:flex-row sm:items-center"><div><h1 className="text-[16px] font-bold">{type.replace(/([A-Z])/g, " $1").trim()}</h1><p className="mt-1 text-[14px] text-slate-400">Manage your catalogue entries.</p></div><div className="flex gap-2"><input ref={csvInput} type="file" accept=".csv" className="hidden" onChange={(event) => importCsv(event.target.files?.[0])}/><button onClick={() => csvInput.current?.click()} className="flex items-center gap-2 rounded-lg border border-[#b13896] px-3 py-2 text-xs font-semibold text-[#b13896]"><Upload size={14}/>Upload CSV</button><button onClick={refresh} className="rounded-lg border border-slate-200 p-2 text-slate-500" title="Refresh"><RefreshCw size={15}/></button><button onClick={openAdd} className="flex items-center gap-2 rounded-lg bg-[#b13896] px-4 py-2 text-xs font-semibold text-white"><Plus size={14}/>Add {singular}</button></div></div><div className="m-5 flex gap-3 rounded-xl border border-slate-100 bg-slate-50/60 p-3"><label className="flex flex-1 items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-2 text-slate-400"><Search size={15}/><input value={search} onChange={(event) => setSearch(event.target.value)} className="w-full bg-transparent text-xs text-slate-700 outline-none" placeholder={`Search ${type.toLowerCase()}...`}/></label><select value={filter} onChange={(event) => setFilter(event.target.value)} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs"><option value="All">All Status</option><option>Active</option><option>Draft</option><option>Trash</option></select><button className="flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-4 text-xs"><Filter size={14}/>Filter</button></div><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-left"><thead className="bg-slate-50 text-[14px] uppercase tracking-wider text-slate-400"><tr><th className="px-6 py-3">Name</th><th className="px-5 py-3">Description</th><th className="px-5 py-3">Status</th><th className="px-6 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-50">{filtered.map((item) => <tr key={item.trashId || item.id} className="text-xs"><td className="px-6 py-4 font-semibold">{item.name}</td><td className="px-5 py-4 text-slate-500">{item.description || "—"}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-[14px] font-semibold ${tone(item.status)}`}>{item.status}</span></td><td className="px-6 py-4"><div className="flex justify-end gap-2">{!item.trashId && <button onClick={() => edit(item)} className="rounded-lg bg-slate-100 p-2"><Edit3 size={14}/></button>}<button onClick={() => item.trashId ? permanentDelete(item) : moveToTrash(item)} className="rounded-lg bg-rose-50 p-2 text-rose-600"><Trash2 size={14}/></button></div></td></tr>)}{filtered.length === 0 && <tr><td colSpan="4" className="px-6 py-14 text-center text-xs text-slate-400">No entries found.</td></tr>}</tbody></table></div></section><section ref={editor} className="rounded-2xl border border-slate-100 bg-white p-6 shadow-sm"><h2 className="text-sm font-bold">{editing ? `Edit ${singular}` : `Add ${singular}`}</h2><form onSubmit={save} className="mt-5 grid gap-4 md:grid-cols-[1fr_1fr_150px_auto]"><label className="text-[14px] font-semibold">{singular} Name *<input value={name} onChange={(event) => setName(event.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-xs"/></label><label className="text-[14px] font-semibold">Description<input value={description} onChange={(event) => setDescription(event.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-xs"/></label><label className="text-[14px] font-semibold">Status<select value={status} onChange={(event) => setStatus(event.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-xs"><option>Active</option><option>Draft</option></select></label><div className="flex items-end gap-2"><button disabled={saving} className="rounded-lg bg-[#b13896] px-4 py-2.5 text-xs font-semibold text-white">{saving ? "Saving..." : editing ? "Update" : "Add"}</button>{editing && <button type="button" onClick={reset} className="rounded-lg border border-slate-200 p-2.5"><X size={15}/></button>}</div></form>{error && <p className="mt-3 rounded-lg bg-rose-50 p-3 text-[14px] text-rose-700">{error}</p>}</section></div>;
};
export default CatalogManager;
